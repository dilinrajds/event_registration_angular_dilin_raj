import { ErrorhighlighterDirective } from './errorhighlighter.directive';

describe('ErrorhighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorhighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
